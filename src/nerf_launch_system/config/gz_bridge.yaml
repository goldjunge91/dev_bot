---
# Nur /clock — die standalone Sim hat keine weiteren Sensor-Topics zu bridgen
# (kein Kamera-/LiDAR-Sensor auf dem Nerf Launcher selbst).
  - topic_name: /clock
    ros_type_name: rosgraph_msgs/msg/Clock
    gz_type_name: gz.msgs.Clock
    direction: GZ_TO_ROS
